void main(){

}
/// An [Interface] can not be inherit or
///  extends from out side of its own library or file 
/// an [Interface] can only implement out side of it own file or library
/// an [interface] can extends and implements from its own file or library
/// an [interface] can not contain [abstract] method because 
/// it is not pure [interface]
/// an [abstract interface class] is pur interface 
interface class Vehicl{
  getSum(){
    
  }
}

class Motor extends Vehicl{

}