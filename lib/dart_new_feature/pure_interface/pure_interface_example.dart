

///Like an [interface] class, other libraries or out side of the file 
/// can implement, but cannot inherit means can't extends, a pure interface
/// inside same file or library we can extends or implements
abstract interface class Shape {
  double calculateArea();
}
